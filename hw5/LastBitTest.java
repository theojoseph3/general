//Checkout

commandInterpreter
isInitialized();
if (args.length == 3) {
    
    File checkoutfile = new File(this._head.find(file name?)); //put in working directory
    this.add(checkoutfile);
    for (File f : this?) {
        if (f.getName() == checkoutfile.getName();
            f.delete();
            .gitlet.add(checkoutfile);
        else {
            continue;
        }
    System.out.println("File does not exist in that commit.");


}
if (args.length == 2) {
    checkoutBranch();
}
if (args.length == 4) {
    File blahblah = new File(find(commit_id...));
    for (File f : this?) {
        if (f.getName() == blahblah.getName();
            f.delete();
            .gitlet.add(blahblah);
        else {
            continue;
        }
    System.out.println("File does not exist.");

    }

    private void checkoutBranch(String branch) {
        for (File file : b.getHead?) {
            if (file.getName() == )
        }
    }
}
